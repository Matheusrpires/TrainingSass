{
  "name": "lescone",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:sass": "node-sass -r --output-style compressed sass -o css",
    "watch:sass": "npm run build:sass && npm run build:sass -- -w"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^4.14.1"
  }
}
